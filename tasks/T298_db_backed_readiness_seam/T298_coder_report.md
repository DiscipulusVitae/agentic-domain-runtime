# T298: DB-backed readiness seam

## Статус: ✅ Завершено

## Пост-фактум сложность
- Оценка Кодера: 5/10
- Кратко: Задача средней сложности. Потребовалось аккуратно реализовать логику GET-запроса через stdlib (urllib), обработать все возможные исключения (сетевые ошибки, ошибки HTTP/API) и не допустить логирования секретов.

## Что сделано
- [x] Добавлена поддержка PERSISTENCE-режимов: memory (по умолчанию) и supabase (через переменную окружения `ADR_PERSISTENCE`).
- [x] Реализована безопасная проверка доступности БД PostgREST в режиме supabase с использованием публично-безопасного эндпоинта (`/rest/v1/persons`) и заголовков `apikey`, `Authorization`, `Accept-Profile`.
- [x] После приёмочного просмотра canonical env для publishable key приведён к `.env.example`: `SUPABASE_API_KEY_PUBLISHABLE`.
- [x] Добавлена защита от утечки секретов: URL и ключи доступа не логируются и не выводятся при ошибках.
- [x] Реализован механизм fail-closed (возврат 503 HTTP-кода и JSON `status: "error"`) при отсутствии обязательных переменных окружения или при сбое связи с Supabase.
- [x] Написаны тесты в `tests/sandbox/test_runtime.py`, покрывающие все сценарии (memory по умолчанию, supabase при отсутствии env, supabase при успешном ответе PostgREST, supabase при сетевом сбое и supabase при ошибке HTTP/API).
- [x] Выполнены доработки по замечаниям (continuation): удалены абсолютные пути из отчёта, обновлена документация по контракту `/health` в `docs/SUPABASE_RENDER_WIRING_RUNBOOK.md`.

## Изменённые файлы
- `[MOD] docs/SUPABASE_RENDER_WIRING_RUNBOOK.md` — обновлена документация по интеграции и контракту готовности базы
- `[MOD] src/sandbox/runtime.py` — реализация логики проверки готовности базы в `/health`
- `[MOD] tests/sandbox/test_runtime.py` — добавлены тесты для `/health`

## Верификация
Вывод запуска тестов (предупреждение VIRTUAL_ENV санировано):
```text
uv run pytest tests/sandbox -q
........................................................................ [ 72%]
...........................                                              [100%]
99 passed in 1.20s
```

Линтер и диффы чистые:
```text
git diff HEAD~1 --check
(вывод пуст)
```

## Проблемы, трудности, неожиданности
> Концевые пробелы в коде и тестах были обнаружены через `git diff --check` и исправлены. Абсолютные пути в выводе тестов санированы.

## Commit Candidate
Локальный commit выполнен (amend).
